export type PassGenOptions = {
    passLength: number,
    lowercase: boolean,
    uppercase: boolean,
    digits: boolean,
    symbols: boolean,
    excludeSimilars: boolean,
    charsToExclude: string
};